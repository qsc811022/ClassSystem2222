✅ 目前進度總結
1. 後端 (Node.js + Express + TS + MySQL + JWT)

✔️ 已完成 Google OAuth 登入 與 JWT 發 token

✔️ 已完成 RBAC 權限控管（student 只能看課程，admin 才能新增課程）

✔️ 已完成 課程 CRUD API（但 student 只有 Read 權限）

✔️ 測試過 Postman → Token 正常攜帶、授權驗證成功

2. 前端 (Vue3 + TS + Pinia + Vite)

✔️ 專案初始化完成 (Vite + Vue + TS)

✔️ 已建立 Pinia store (auth.ts)，可以保存 token & user

✔️ 已建立 Axios 攔截器 (api.ts)，自動帶上 JWT Token

✔️ 已完成 登入畫面 (Login.vue)

可以用帳號密碼登入

已加入 Google 登入按鈕（會導向後端的 /auth/google）

✔️ 已完成 課程清單頁面 (CourseList.vue)

只有登入後才能看到課程

student role → 只能看

admin role → 可以新增課程 (後端 API 已支援)

🔜 下一步可以做的功能

前端頁面導向

登入成功 → 自動跳轉 /courses

未登入 → 自動跳回 /login

顯示使用者資訊

在頁面右上角顯示當前登入者的名字 & role

加上「登出」按鈕，清空 store.token 與 store.user

課程管理功能 (Admin)

新增課程的表單頁面

刪除/修改課程

美化畫面 (UI)

用 Tailwind / Element Plus / Naive UI 美化目前的 Login 與課程清單

Google 登入按鈕換成漂亮的樣式（Google 標準登入按鈕）


好 👍 在 commit 之前，我幫你整理一下目前專案狀態，這樣 commit message 才會清楚。

📌 專案目前進度整理
✅ 後端 (backend)

已建立 Express + Sequelize 架構

已完成 User Model，欄位包含 user_id, google_id, username, email, password, role, name, created_at

已新增 註冊 API (POST /auth/register)

檢查 Email 是否重複

Hash 密碼並存入資料庫

已整合 Passport.js（Google 登入路由已掛載）

已安裝並配置 cors（但需要統一路徑 /api/... 或直接 /auth/...）

✅ 前端 (frontend - Vue3 + TS + Vite + Pinia)

已建立基本頁面結構 (Login.vue, Register.vue, CourseList.vue)

已完成登入流程（JWT token 存入 Pinia）

登入成功後自動跳轉 /courses

未登入訪問 /courses 時會被導向 /login

已新增 註冊畫面 (Register.vue)，可輸入名稱、Email、密碼

目前請求會發送，但因為路由 prefix (/api) 與後端不一致，導致 404 Not Found

🔧 下一步建議

修正 API 路由統一性

後端改成 app.use("/api/auth", authRoutes);

前端保持 api.post("/auth/register")

測試註冊 API

確保新用戶能被正確存入 DB。

Navbar

登入後右上角顯示 username + role

新增「登出」按鈕，清空 Pinia store。

✅ Commit 建議
git add .
git commit -m "feat: 新增註冊功能 (前後端串接中)，調整 User Model 與前端頁面"